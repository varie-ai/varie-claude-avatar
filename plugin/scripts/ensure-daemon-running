#!/bin/bash
# Compatibility wrapper for ensure-daemon-running (deprecated: hooks now invoke node hook client directly)
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
exec node "$SCRIPT_DIR/varie-avatar-hook.cjs" session-start --ensure
